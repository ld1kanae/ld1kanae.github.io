name: DrumScribe hat context production v58

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "drumscribe/transcribe.js"
      - "drumscribe/hat-context-v57.js"
      - "drumscribe/experiments/browser_hat_context_production_v58.mjs"
      - "drumscribe/experiments/evaluate_hat_context_production_v58.py"
      - ".github/workflows/drumscribe-hat-context-production-v58.yml"

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: |
          pip install mido
          npm install --no-save playwright
          npx playwright install --with-deps chromium
      - name: Serve repository
        run: |
          python -m http.server 8000 >/tmp/drumscribe-http.log 2>&1 &
          sleep 2
      - name: Generate production/off predictions
        run: node drumscribe/experiments/browser_hat_context_production_v58.mjs
      - name: Score production wiring
        run: python drumscribe/experiments/evaluate_hat_context_production_v58.py
      - name: Commit results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add drumscribe/experiments/results-hat-context-production-predictions-v58.json
          git add drumscribe/experiments/results-hat-context-production-v58.json
          git add drumscribe/experiments/HAT_CONTEXT_PRODUCTION_V58.md
          if git diff --cached --quiet; then
            echo "No result changes"
          else
            git commit -m "drumscribe: record production hat context v58 results"
            git pull --rebase origin main
            git push
          fi
      - uses: actions/upload-artifact@v4
        with:
          name: drumscribe-hat-context-production-v58
          path: |
            drumscribe/experiments/results-hat-context-production-predictions-v58.json
            drumscribe/experiments/results-hat-context-production-v58.json
            drumscribe/experiments/HAT_CONTEXT_PRODUCTION_V58.md
          retention-days: 14
