name: DrumScribe hat sync promote v67

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "drumscribe/hat-context-v57.js"
      - "drumscribe/models/hat-sync-candidate-rf-v66.json"
      - "drumscribe/experiments/browser_hat_sync_candidate_promote_v67.mjs"
      - "drumscribe/experiments/evaluate_hat_sync_candidate_promote_v67.py"
      - ".github/workflows/drumscribe-hat-sync-promote-v67.yml"

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: |
          pip install mido
          npm install --no-save playwright
          npx playwright install --with-deps chromium
      - name: Serve repository
        run: |
          python -m http.server 8000 >/tmp/drumscribe-http.log 2>&1 &
          sleep 2
      - name: Generate promote-only replay
        run: node drumscribe/experiments/browser_hat_sync_candidate_promote_v67.mjs
      - name: Score five-song production guard
        run: python drumscribe/experiments/evaluate_hat_sync_candidate_promote_v67.py
      - name: Commit reproducible results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add drumscribe/experiments/results-hat-sync-candidate-promote-predictions-v67.json
          git add drumscribe/experiments/results-hat-sync-candidate-promote-v67.json
          git add drumscribe/experiments/HAT_SYNC_CANDIDATE_PROMOTE_V67.md
          if git diff --cached --quiet; then
            echo "No result changes"
          else
            git commit -m "drumscribe: record promote-only synchronized hat v67 replay"
            git pull --rebase origin main
            git push
          fi
      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: drumscribe-hat-sync-promote-v67
          path: |
            drumscribe/experiments/results-hat-sync-candidate-promote-predictions-v67.json
            drumscribe/experiments/results-hat-sync-candidate-promote-v67.json
            drumscribe/experiments/HAT_SYNC_CANDIDATE_PROMOTE_V67.md
          retention-days: 14
